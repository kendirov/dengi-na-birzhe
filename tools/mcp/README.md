# Future MCP Servers

Placeholder directory for local MCP implementations referenced in `.cursor/mcp/mcp.template.json`.

## moex-market-data-mcp

Wrap existing `lib/data/` as MCP tools:
- `get_instruments`
- `get_debug_diagnostics`
- `get_ticker`

Status: **not implemented**

## telegram-source-mcp

Read-only access to Telegram channel exports / bot API for news briefs.

Status: **not implemented**

Do not add tokens to git. See `.cursor/mcp/README.md`.
